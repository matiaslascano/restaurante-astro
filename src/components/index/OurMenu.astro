---
import { menu } from "../../constants";
---

<main class="w-full md:h-[400px] md:justify-center md:gap-10 flex flex-col mt-20 bg-white py-4 px-4 md:py-60">
  <div class="mb-6 text-center md:flex md:flex-col md:gap-8">
    <h2 class="text-4xl md:text-6xl">Menú Destacado</h2>
    <p>This is a section of your menu. Give your section a brief description</p>
  </div>
  <div
    class="grid grid-cols-1 gap-8 sm:grid-cols-2 md:grid-cols-2 xl:grid-cols-4  md:max-w-[1200px] md:mx-auto"
  >
    {
      menu.map((menu) => (
        <div class="border-t-4 border-dotted border-black p-4 md:flex md:flex-col md:justify-between">
          <h3 class="text-3xl">{menu.name}</h3>
          <p class="text-md">{menu.description}</p>
          <p class="text-right text-2xl font-bold">${menu.price}</p>
        </div>
      ))
    }
  </div>
</main>
